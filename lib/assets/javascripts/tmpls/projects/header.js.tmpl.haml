.header
  %h2
    {{if current_user_id && current_user_id == owner_id}} Your {{/if}}
    Projects
  {{if current_user_id && current_user_id == owner_id}}
  %a.button.complement(href='/projects/new') New Project
  {{/if}}
  %a.button.complement(href='/projects?filter=tutorial') Tutorials

