.header
  %h2
    {{if current_user_id && current_user_id == sprite_owner_id}} Your {{/if}}
    Sprites
  {{if current_user_id && current_user_id == sprite_owner_id}}
  %a.button.complement(href="/pixel-editor") New Sprite
  {{/if}}
  %img.spinner(src='/assets/ajax-loader.gif')
.pagination
