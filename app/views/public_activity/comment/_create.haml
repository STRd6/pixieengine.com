-#TODO Should make activity dependent destroy to avoid this
- if (comment = activity.trackable) && comment.commentee && comment.commenter
  %comment
    %header
      - if commentable = comment.commentable
        - if commentable.is_a? Sprite
          = render commentable
        - elsif commentable.is_a? Tune
          %tunes.inline
            = render commentable
      = avatar_link comment.commentee
      = avatar_link comment.commenter
    %section
      %header><
        = link_to comment.commenter.display_name, comment.commenter, rel: "author"
        &nbsp; commented on&nbsp;
        = link_to comment.commentee.display_name, comment.commentee, rel: "receiver"
        &#39;s #{comment.commentable_type}&nbsp;
        = link_to "#{time_ago_in_words(comment.created_at)} ago", comment.commentable, class: "time"

        = markdown(comment.body)
