- if i > 67
  .sprite_container{ :class => "extras"}
    = link_to image_tag(sprite.image.url(:thumb), :draggable => true, "data-name" => sprite.display_name, :alt => sprite.display_name, :title => sprite.display_name), sprite
- else
  .sprite_container
    = link_to image_tag(sprite.image.url(:thumb), :draggable => true, "data-name" => sprite.display_name, :alt => sprite.display_name, :title => sprite.display_name), sprite