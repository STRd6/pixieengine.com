- link_path ||= sprite_path(sprite)
.sprite_container
  - if admin?
    = form_for sprite, url: { id: sprite.id, controller: :sprites, action: :suppress } do |form|
      = form.button "🔨"
  = link_to image_tag(sprite.image_url, :alt => sprite.display_name, :width => sprite.width, :height => sprite.height), link_path
