%header
  #flashes
    - flash.each do |key, msg|
      %div{ :class => key }= msg

  = link_to image_tag('/images/pixie.png', :alt => "Home", :title => "Home", :id => "logo"), root_path

  - unless @hide_bulb
    .bulb

  = render :partial => "/shared/login"
