- if current_user
  == Logged in as #{link_to current_user, current_user }
  (#{link_to "Log out", logout_path, { :title => "Log out" }})

-else
  /= link_to "Sign Up", sign_up_path, {:title => "Sign Up"}
  /or
  = link_to "Log in",  login_path,  { :title => "Log in" }
