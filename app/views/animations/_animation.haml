.animation.clickable
  .title_container
    %h2.title= link_to animation.name, edit_animation_path(animation)
    %span.author
      by
      =animation.user.display_name
  -animation.preview_urls.each do |preview|
    .sprite_container
      .name
        = preview[0]
      - if preview[1][0..13] == "data:image/png"
        %img{ :src => preview[1] }
      - else
        = image_tag(preview[1])
