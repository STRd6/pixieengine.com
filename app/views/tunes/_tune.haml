- link_path ||= tune_path(tune)

%card
  .outline
    .details
      .title= tune.display_title
      .description= markdown tune.description
      .actions
        %a(href="#{link_path}")
          = pluralize(tune.score, "Reax")
          👍
        %a(href="#{link_path}")
          = pluralize(tune.comments_count, "Comment")
          💬
  %creator
    = link_to user_path(tune.user) do
      = image_tag(tune.creator_thumb_url, :alt => tune.creator_name)
      = tune.creator_name
