.comment{ 'data-commentable_id' => "#{comment.commentable_id}" }
  %p= comment.body
  %p.attribution
    %span.name= comment.commenter.display_name
    %span.time= "#{time_ago_in_words(comment.created_at)} ago."
