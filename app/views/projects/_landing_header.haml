%header.navbar(eventCategory="Header")
  #flashes
    - flash.each do |key, msg|
      %div{ :class => key }= msg

  .navbar-inner
    .container
      %span.landing_logo
      %h2.blurb
        The online game development toolkit

      = render :partial => "/shared/login"
