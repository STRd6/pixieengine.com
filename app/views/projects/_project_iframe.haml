- config = project.config
%iframe#app.no_border{ :width => config[:width], :height => config[:height], :src => widget_project_url(project, :host => "games.pixieengine.com")}

- content_for :javascript do
  :javascript
    // Set focus
    $("#app")[0].contentWindow.focus();
