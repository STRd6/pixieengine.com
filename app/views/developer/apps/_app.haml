.app
  %h2.title= link_to app.title, [:developer, app]
  .thumb= link_to image_tag(app.image.url(:thumb), :alt => app.display_name, :title => app.display_name), [:developer, app]
  %p.description= app.description
